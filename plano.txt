- gerar watch no grunt
- um JS no service para fazer as requisições, todos passam por ele (inclur o bodyRequest bele)
- usar outro datetimepicker
- enviar nome da coluna para ordenar e não a posição dela
- nome do JS compilado deve ser main.js no lugar de <modulo>.js
- dao e model ficar num package geral, não do modulo
- Usar OpServlet e não colocar OpResponse no process
- usar vue.js
- se a coluna for "ativo" poder inativa-la e não exibir a linha inativa no listar
- se a coluna for "email" deve validar como e-mail no persist
- se a coluna for "password" deve fazer sha1 no js e sha1 no insert/update; se estiver em branco no update não alterar; não exibir no listar
- se a coluna for "imageUrl" deve exibir uma imagem no listar
- se a coluna for "url" deve validar no persist; no listar, ao clicar deve abrir nova aba
- smartType para colocar NOW() no insert
- ler comentario contendo "martinlabs-generator:ignore" e ignorar coluna
- gerar testes unitários

- atualizar o pom com martinlabs java commons, versão de java 1.8, remover node_modules
- exportar em csv da tabela inteira (não só da tabela exibida)
- em todos os WS além de token tem que enviar versão do client para validar se é muito antiga e sistema operacional
- gerar "esqueci a senha"
- ideia do koba: separar packages do ws em list, get, update
- poder alterar "minha conta"
- gerar create.sql