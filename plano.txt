- perguntar user, senha, banco de dados, tabela e datasource
	http://yeoman.io/authoring/user-interactions.html
- permitir não informar a tabela e usar todas as tabelas
- perguntar se quer incluir sistema de login, se sim perguntar tabela coluna de login e coluna de senha
- gravar preferencias em um arquivo na pasta e ler para deixar de default no prompt
- ler campos da tabela pegando nome e tipo e colocando em uma lista
	https://github.com/melanke/SkillHunter/blob/master/server/dao/PlayerDao.js
	SELECT 
	column_name, is_nullable, data_type, character_maximum_length 
	FROM information_schema.columns 
	WHERE table_schema = "apptite" 
	AND table_name = "usuario"
	AND extra != "auto_increment";
- ler pom.xml e pegar o groupId
	https://github.com/Leonidas-from-XIV/node-xml2js
- criar model com nome da tabela com propriedade dos campos, substituir se já existir
	yomen copyTpl
- criar dao com o nome da tabela colocando métodos de insert, update, exist e list com os campos da tabela e do model, substituir se já existir
- criar process do nome da tabela com métodos de persistir e listar, substituir se já existir
- criar WS de persistir e listar usando o model e o process, substituir se já existir
- criar arquivo de URL dentro de const com URLs dos WS, ou atualiza-lo
- criar arquivo de rotas em js, ou atualiza-lo
- criar arquivo listar<nome da tabela>.js
- criar arquivo default.scss que importa o bootstrap, pular se já existir
- criar arquivo default.html dentro de tmpl, deixar ler URL.js em runtime para saber quais páginas estarão no menu, pular se já existir
- criar arquivo listar<nome da tabela>.html
- criar arquivo persistir<nome da tabela>.js dentro do controller, substituir se já existir
- criar arquivo persistir<nome da tabela>.html
- criar arquivos para tela home

- atualizar Gruntfile para incluir caminhos do crud
	http://yeoman.io/authoring/gruntfile.html
- rodar grunt
	this.spawnCommand('grunt', ['taskname']);

NOVAS IDEIAS:
- mutiplas linguas
- login mais seguro

BUG:
- se a tabela tiver 2 primarykey vai fazer select e update por 1 id só